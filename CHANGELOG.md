# 판 이력

*"`v0.9.3` 이 뭐였지"* 를 한 곳에서 답하려고 둔다.

- **왜 그렇게 했는지**는 `devlog/` — 괄호 안의 번호가 그 문서다
- **그 며칠에 무슨 일이 있었는지**는 `docs/*-progress-*.md` (진척 보고서)
- **지금 무엇이 도는지**는 [HANDOFF.md](HANDOFF.md) 와 `/healthz` — **이 파일은
  지금을 말하지 않는다**

**뷰어와 파이프라인은 판이 따로다**(`IMAGE_TAG` / `PIPELINE_TAG`). 하나로 묶었다가
폴러가 4시간 반 멈춘 적이 있다(026). 다만 **스키마를 조이는 마이그레이션**과
**슬러그·경로 같은 "값의 모양" 규칙**은 둘을 함께 올려야 한다.

**뷰어가 판을 자주 낸다** — 아래가 본줄기이고, 파이프라인은 뒤에 따로 적는다.
**git 태그가 있는 것은 뷰어뿐이다**(`v0.8.0` 부터 죽 있다). 파이프라인 판은
이미지 태그로만 있고, 그 앞은 태그가 없다. **`v0.12.0` 부터 이미지를 CI 가
민다** — 그 앞은 태그를 붙이고도 사람이 손으로 굽고 올렸다(114 7절).

**내보내는 절차는 [docs/20260813_release-flow.md](docs/20260813_release-flow.md)
다** — 이미지는 **태그를 밀 때만** CI 가 굽고 Docker Hub 로 올린다.

---

## 뷰어

### `v0.23.2` — 09-07 (마이그레이션 없음)

**고르는 일이 화면을 넘어 산다 — 그리고 고르는 중에는 카드가 고르는 자리다**
([187](devlog/20260907_187_catalog-pick-survives-reload.md) ·
[188](devlog/20260907_188_catalog-pick-mode-locks.md))

185 가 「이 쪽 전부 고르기」를 놓아 한 번에 120개를 고를 수 있게 되면서, 고른
것을 **잃는 자리** 둘이 드러났다. 서버는 안 고쳤다.

- **새로고침이 고른 것을 비웠다**(187). `base.html` 은 뒤로 와서 되살아난
  화면(bfcache)을 늘 다시 받는데 — 교정·검토 표시가 옛것이면 안 되므로 —
  고른 것은 DOM 에만 있어 그때 사라졌다. 되받기는 그대로 두고 고른 것을
  `sessionStorage` 로 넘긴다. **주소가 같을 때만 되살린다**: 거르개·쪽이
  달라지면 그 쪽에 없는 카드를 고른 채로 두는 셈이다
- **고르는 중에는 편집과 시야로 가는 길을 잠근다**(188 · 사용자 요청). 카드는
  440px 인데 고르는 칸은 왼쪽 위 13px 이라, 빗나가면 시야로 넘어가거나 일괄로
  앉힐 값이 그 개체에만 먼저 앉았다. 이제 카드를 누르면 골라진다 — 마우스는
  `pointer-events`, 키보드는 내려가는 쪽에서 클릭을 가로채서 막는다.
  잠근 것은 띠에 글자로 적는다

**도감에 「작업 이름」이 선다 — `Chaetoceros spp.`**
([186](devlog/20260906_186_working-names-atlas.md))

종까지 못 내리고 속에서 멈춘 동정에 적는 이름이다. 색인 어디에도 없는 이름이라
`check_db` 11번이 세는데, 185 의 일괄로 `Chaetoceros` 개체 전부에 앉히면
**경고가 1,166건**이 될 참이었다.

- **도감 하나를 따로 세웠다**(`atlas/working-names.json`) — 출판된 색인에
  없는 표제어를 끼우면 인용이 원문과 어긋난다. 도판이 없어 `/atlas/` 에
  카드가 서지 않고 검색에서만 걸린다
- **자리 검사에서 뺐다**(`extra.working_name`) — 도판 없는 것이 정상인
  항목이다. **뺐다고 화면에 적는다**
- 파일은 이 판이 이미지에 싣는 것이고, **운영 DB 에는 09-06 에 `--src` 로
  먼저 반입해 두었다**

### `v0.23.1` — 09-06 (마이그레이션 없음)

**카탈로그에 「이 쪽 전부 고르기」를 놓는다**
([185](devlog/20260906_185_catalog-pick-page.md))

일괄 저장(P16 5.2)은 있었는데 **고르는 수단이 카드마다의 체크박스뿐이었다** —
유형 하나에 종명을 앉히려면 한 쪽 120번을 누르게 된다. 유형 `chaetoceros`
개체 203개가 그 자리였다. **서버는 안 고쳤다.**

- **쪽 밖으로 안 넘는다.** `bulk` 의 상한 옆에 적힌 "화면에 없는 것을 고를
  수는 없다" 를 그대로 지킨다 — 거르개가 걸린 채 쪽을 넘어 고르면 사람이
  안 본 카드에 종명이 앉는다. 몇 개를 고르는지 단추에 적는다
- **단추 하나가 두 방향으로 돈다.** 다 골라져 있으면 「이 쪽 고르기 해제」이고,
  반쯤 고른 채로 누르면 나머지를 채운다(푸는 쪽으로 돌면 방금 고른 것이 사라진다)
- `.chip` 은 `<a>` 의 껍데기라 배경도 글꼴도 없었다 — `button.chip` 으로
  되돌렸다. 이 파일에 이미 적혀 있던 경고를 밟은 자리다

### `v0.23.0` — 09-06 (마이그레이션 없음)

**검토 화면 오른쪽 칸이 개체 카탈로그가 된다**
([183](devlog/20260904_183_catalog-pane.md))

마스크를 하나 고르면 그 개체의 카드가 오른쪽에 뜬다 — 번호·종명·유형·등급·
자세·코멘트·묶음 판 수, 그리고 「카드로 →」. **그 자리에서 바로 적는다.**
지금까지는 동정하려면 카탈로그 화면으로 나가야 했고, 나가면 보던 시야를
잃었다(개체 14,074개 중 종명이 적힌 것이 41개였다).

- **저장 문이 둘이다.** 종명·등급·자세·코멘트는 `/catalog/save`(개체 하나만
  고치는 좁은 문)로, **유형은 이 화면의 `labels`** 로 간다 — 그 칸은 판
  payload 가 통째로 보내는 값이라 다른 문으로 적으면 다음 저장이 지운다(104)
- **번호는 앵커에서 뽑는다** — 카탈로그 화면과 같은 규칙이라 두 자리에서 같은
  값이다. 처음 적어 카드가 그 순간 생기면 저장 응답이 번호를 들고 온다
- **시야 메모 상자를 걷었다.** 570 시야에 6건이었다 — **화면에서만** 걷었고
  DB 의 기록과 서버의 문(`{"only": "note"}`)은 그대로다

**묶인 개체의 유형이 판마다 따로 놀던 것을 접는다**
([182](devlog/20260904_182_fold-object-cls.md))

유형을 아직 지정하지 않은 개체는 화면이 판마다 엔진 값으로 떨어져 같은
규조각의 색이 판을 넘길 때마다 바뀌었다(실측 19개). 아는 판의 값을 개체
전부가 쓴다. **접은 값은 사람의 지정이 아니라 저장에 실리지 않는다.**

### `v0.22.5` — 09-03 (마이그레이션 없음)

**조용히 자료를 잃는 갈래 다섯을 잡는다**
([180](devlog/20260903_180_silent-loss-sweep.md))

`v0.22.4` 를 고치고 나서 같은 계열을 훑어 나온 것들이다. **지금 자료로는
아직 안 밟히지만 전부 사람의 판단이 소리 없이 사라지는 자리다.**

- **완료·시야 코멘트를 판 payload 에서 뗐다.** 둘 다 시야에 붙는 것인데
  마스크 저장에 실려 다녔다 — 같은 시야를 두 탭으로 열면 한쪽에서 켠 완료와
  적은 글을 다른 탭의 저장 한 번이 되돌렸다. 코멘트에도 완료와 같은 문을
  냈고(`{"only": "note"}`), 서버는 **안 실린 것을 "안 건드린다" 로 읽는다**.
  옛 탭이 싣고 오면 그때는 쓴다. 확인 표시(`confirm_kept`)는 완료를 누르는
  자리로 옮겼다
- **검출이 없는 판에는 저장하지 않는다.** 그 판을 고르면 `image` 가 비어
  저장이 **합성본**으로 가서 그 판의 교정을 갈아치웠다 — 화면은 "저장됨"
  이라고 적었다. 116 이 완료만 떼어 낸 자리의 나머지 절반이다
- **이미 저장된 기하를 매번 다시 검사하지 않는다.** `--scale` 을 빠뜨린
  재검출로 판 크기가 줄면 고쳐 둔 기하가 전부 밖이 되어 **그 판이
  새로고침해도 영영 저장 불가**였다. 새로 고치는 것은 그대로 검사한다
- **"↺ 엔진이 낸 모양으로" 를 되돌릴 원본이 있을 때만 낸다.** 고아 개체에서
  누르면 409 뒤 곧바로 새로고침이라 저장 안 된 나머지까지 사라졌다
- **카탈로그의 지우기로 처음 생기는 줄도 기하를 들고 앉는다.**
  `update_fields` 에 `geom` 이 빠져 있었다

### `v0.22.4` — 09-03 (마이그레이션 없음)

**그린 개체를 지워도 검토 저장이 안 막히게 한다**
([179](devlog/20260903_179_review-save-rejected.md))

실사용에서 하루에 교정 101건이 이렇게 날아갔다 — 그린 개체를 하나 지우면
그 시야의 이후 저장이 전부 409 로 거절돼서, 사람은 같은 검토를 몇 번이고
다시 했다. **완료 표시만 따로 가서 200 이 되므로 화면은 검토 완료인데
교정은 하나도 안 남는다.**

- 그린 개체(`batch=NULL`)의 키가 `removed`·`accepted`·`labels` 에 실려
  나가면 서버가 "현재 검출에 없는 개체" 로 **저장 전체를 거절**했다.
  화면은 그 키를 안 싣고(지운 것은 `drawn` 에서 뺀다), 서버는 와도 흘린다
  — 옛 탭의 `notes` 와 같은 갈래다
- 그린 폴리곤이 이미지 밖으로 나가도 같은 자리에서 저장 전체가 거절된다.
  **만들 때 막는다** — 그리기·점 끌기·점 넣기가 좌표를 이미지 안으로 들인다
- 앉히기·묶기가 `geom` 을 `bbox_xywh` 로 적어 `_orphan_dict` 가 못 읽었다.
  못 읽으면 그 개체가 화면에 안 그려지고 **다음 저장이 그 행을 지운다**.
  쓰는 자리를 `bbox` 로 맞추고 읽는 자리는 옛 모양도 받는다(그렇게 앉은
  행이 2,875건)
- 브라우저 시험 하나가 **이 고장 덕분에 통과하고 있었다** — 저장이 거절되니
  "복제본이 안 지워졌다" 가 참이 됐다. 저장을 일으키는 자리를 옮겼다

### `v0.22.3` — 09-01 (마이그레이션 없음)

**도감 영문 토글이 `<main>` 보다 먼저 도는 고장을 고친다**

`v0.22.2` 의 `_atlaslang.html` 스크립트가 `{% block nav %}` 안, 검색 폼·
칩·본문(`<main>`)보다 앞에서 곧바로 돌았다 — 첫 `apply()` 가 실행되는
시점에 `<main>` 의 `[data-en]` 요소가 아직 DOM 에 없어 번역을 못 찾았다.
`localStorage` 에 `lang=en` 이 남아 있어도 화면은 그대로 한국어로 뜨고,
그 상태에서 토글을 누르면 그제서야 다 갖춰진 DOM 에 한꺼번에 적용돼
**거꾸로 도는 것처럼 보였다**(실사용 2026-09-01, "Search 를 눌렀더니
찾는다 로 바뀐다").

- `_atlasview.html` 이 이미 쓰는 DOMContentLoaded 지연 패턴을 그대로
  따랐다
- playwright 로 옛 코드에서 재현하고, 고친 코드로 페이지를 새로 열어도·
  넘나들어도 상태가 지켜지는 것을 확인했다

### `v0.22.2` — 09-01 (마이그레이션 없음)

**도감 화면에 영문 토글을 달고 머리줄 도감 단추를 꾸민다**

- 도감 도판 화면 넷(`atlas`·`atlas_volume`·`atlas_page`·`atlas_spread`)이
  함께 쓰는 `_atlaslang.html` 로 **화면 UI 문구만** 영문으로 바꾸는 토글을
  넣었다 — 도판 이미지·색인 원문(캡션·생태·분포·이명 등)은 그대로 둔다.
  `localStorage`(`diaruga.atlas.lang`)로 도감 화면을 넘나들며 상태를 지킨다
- 머리줄의 "도감" 링크가 꾸미지 않은 글자 링크였던 것을 테두리 있는 단추로
  바꾸고 표기를 "Atlas" 로 옮겼다(사용자 2026-09-01)

### `v0.22.1` — 08-31 (마이그레이션 없음)

**도감 목록을 다권 도감과 논문 도판집으로 가른다** (178)

도감이 15건으로 늘면서 `/atlas/` 기본 화면이 표지 있는 다권 도감과 표지
없는 논문 도판 발췌본을 같은 무게로 줄 세우고 있었다. 진짜 다권 도감은
`korean`·`schmidt` 둘뿐이다(east-antarctic 은 title 이 이미 "도판집" 이라
적힌 논문 발췌본 — 어느 파서가 만들었는지로 가르면 이 하나가 틀린다).

- `views.atlas_index` 에 `_BOOK_ATLAS_CODES` 를 못 박고 목록을
  `book_atlases`(2)/`paper_atlases`(13)로 갈라 냈다 — 사람이 정한 분류라
  `atlas.py`("파일이 곧 자료")나 `Atlas` 모델 어디에도 안 담는다
- 논문 도판집 13건은 표지 큰 행 대신 "논문 도판집" 소제목 아래 작은
  그리드로 접었다

### `v0.22.0` — 08-31 (마이그레이션 0043 · `TaxonName`)

**학명 유효성 판정을 도감 검색에 넣는다** (P24)

P15 §8.1 이 "방침이라 admin 이 정한다" 며 미뤄 둔 `TaxonName` 을 만든다.
계기는 사용자가 `/atlas/` 에서 옛 이름(`Actinocyclus Ehrenbergii`)으로
찾다가 빈 결과를 본 것 — 그 판정은 이미 `worms_master_20260814.tsv`
(도감 1,845종 마스터 표, 08-14~08-26)에 있었다.

- `worms_master_20260814.tsv`(도감 1,845종)와 논문 도판 156종의 AlgaeBase
  조회 결과(사람이 철자를 먼저 교정하고 조회한 것)를 합쳐 반입 —
  **1,999건**(accepted 995 · synonym 740 · unassessed 145 · absent 119)
- **양방향 검색**: 옛 표기로 찾으면 결과 카드에 "→ 현재 통용 학명" 이
  함께 뜨고, **현재 학명으로 찾아도 도감의 옛 표기 항목을 찾아낸다**
  (`Actinocyclus octonarius` 로 찾으면 도감 3편·논문 5편의
  `Actinocyclus Ehrenbergii` 항목이 걸린다 — 이게 사용자가 겪은 자리다)
- `TaxonName` 은 `Atlas` 와 같은 자리다 — 원본은 NAS(md/tsv/json), DB 는
  거기서 뽑은 사본. 화면에서 판정을 고치는 UI 는 없다

### `v0.21.0` — 08-31 (마이그레이션 0042 · `AtlasPlacement.crop_image`)

**논문 도판 8편을 도감 표에 더 얹고, 개체 크롭을 화면에 낸다** (P22·P23)

169~177 이 신생대 규조 논문 8편(2017 울릉분지·1994 남양만·1993 남해·2001
브랜스필드·1975 포항감포·1986 남한 신제3기·1996 브랜스필드·1991 연일층군)의
도판 그림 440개를 잘라 캡션 학명을 원문 대조로 확정했다. **P23** 이 이것을
기존 도감 반입 파이프라인(`tools/parse_paper_atlas.py`·`ops/import_atlas.py`
— `v0.20.0` 에서 논문 넷을 이미 이 문으로 넣었다)에 그대로 얹었다 — 새
임포터를 안 만들었다.

- **`AtlasPlacement.crop_image`** 칸을 새로 더해(0042) 그림 하나 단위의
  크롭 경로를 담는다 — 이 8편은 범위(`fig. 6-8`)가 아니라 **그림 하나 =
  자리 하나**로 반입한다(같은 종이 한 판에 여러 번 나와도 크롭이 갈린다)
- 크롭 PNG 440장은 `/data3/DiaRUGA/atlas/<key>/crops/` 로 미리 옮겨 뒀다
  (`tools/sync_paper_plate_images.py`, NAS→`/data3`, 배포 전 별도 실행 —
  이미지가 아니라 DB 반입만 이 판을 기다린다)
- 도감 검색(`/atlas/?q=`) 결과 카드에 **"그림 보기" 칩**을 더했다 — 마우스를
  올리면 도판 쪽 전체가 아니라 **그 그림 하나만** 미리 보인다. 141 의
  `data-prev` 미리보기가 이미 범용이라 **JS 는 한 줄도 안 고쳤다**
- **도감 검색 결과에 출현 기록을 붙였다** (168) — `Reference`·`Occurrence`
  (`v0.20.0` 의 0041)가 반입까지 끝났는데 화면 어디에도 안 붙어 있던 것을
  이번에 잇는다. 종 하나의 검색 카드에 "출현: 지역(문헌, 연도)" 줄이 늘었다
  (출현 기록이 없으면 그 줄 자체를 안 낸다)

**운영 반입은 이 판 배포 뒤에 한다** — `atlas/*.json` 8개가 이미지에 처음
실리므로 `ops/import_atlas.py` 를 다시 돌려야 새 논문 여덟이 `/atlas/?q=`
에 걸린다(HANDOFF 3.8). **1936·1992·1993(Chaetoceros)·1985 넷은 크롭
접두사·개수가 안 맞아 이번엔 `crop_image` 를 안 채웠다** — 남은 일.

### `v0.20.0` — 08-27 (마이그레이션 0041 · `Reference`·`Occurrence`)

**논문 도감 넷을 도감(Atlas) 표에 얹는다** (163~166)

한국 규조 논문 넷(1936 Skvortzov·1985 Akiba·Yanagisawa·1992 이·1993 이)의
도판 1,078장을 기존 도감 셋(한국·Schmidt·동남극)과 같은 `Atlas`/
`AtlasEntry`/`AtlasPlacement` 표로 옮겼다 — 도감·논문이 뷰어에서 한 갈래로
열린다. 도판 이미지도 같은 자리(`/data3/DiaRUGA/atlas/<code>/`)에 구웠다(166).

- **출현 기록 층을 새로 만들었다** (164, 0041) — `Reference`(문헌)·
  `Occurrence`(도감·논문의 분포 문장, 한국 도감 924건). `AtlasEntry` 에
  FK 를 매달지 않는다 — `source`·`binomial` 문자열로 도감과 느슨하게 맞춘다
- **`Atlas.key` 를 내부 파일-스템과 갈랐다** (165) — 밑줄 있는 내부 키는
  URL 조각으로 못 쓴다. 논문 넷의 공개 키를 `1936-skvortzov` 모양으로 새로
  정했다(반입 전에 잡아 DB 마이그레이션은 필요 없었다)
- 논문은 펼침(spread)의 근거가 없어(저널 낱장을 이은 스캔) 도판을 격자로만
  본다 — `LEFT_PARITY` 는 죽지 않게 `"odd"` 로만 채웠다

**운영 반입은 이 판에서 한다** — 이미지에 논문 도감 JSON 이 처음 실리므로
`import_atlas.py` 가 이제야 넷을 잡는다(`ops/import_atlas.py`·
`ops/import_occurrence.py`, `check_db.py` 11·12번).

### `v0.19.0` — 08-26 (마이그레이션 없음)

**검출 마스크를 같은 시야의 다른 판에도 앉힌다** (P19 · 157)

검출이 한두 판에서만 훌륭하게 잡히는 일이 있다. 카드 하나가 개체 하나가 된
뒤로(P18) 프레임에서만 잡힌 규조각도 카탈로그에 나오는데, **멤버가 하나뿐이면
그 흐린 프레임이 곧 얼굴이다.** 잘 잡힌 마스크를 합성본에 앉히고 묶으면 얼굴이
선다.

- 우클릭 **`⧸ 다른 판에도 앉히기 (N)`** — 괄호 안이 앉힐 판 수이고 0이면 안 뜬다
- `POST /d/<slug>/g/<gid>/spread` — `/review` 에 안 싣는다(층이 다르다 · 116)
- 복제본은 **사람이 그린 마스크**(`batch=None`)로 새 손그림 키를 받는다.
  원본 판정은 회차에 붙은 채 그대로 있다
- 얼굴은 **혼자인 개체일 때만** 합성본으로 옮긴다 — 사람이 ★ 로 고른 것은
  안 건드린다
- **겹치는 검출은 안 본다**(사용자 방침) — 묶을지 앉힐지는 사람이 정한다

**함께 고친 것** — 앉힌 뒤 그 판에서 저장하면 옛 번지기(`_spread_drawn`)가
복제본을 **원본이 앉은 판으로 되번져** `(개체, 이미지)` 유일 제약에 걸려
**저장이 500** 이었다. 섞인 개체(판정 하나 + 그린 마스크 여럿)는 이번에 처음
생긴 모양이라 그 갈래가 없었다. 브라우저 시험이 잡았다.

**덤** — 검토 화면의 띠(`errbar`)가 스타일이 없어 맨 글자로 뜨고 있었다.
렌더한 인라인 스크립트를 `node --check` 로 파싱하는 시험이 생겼다.

시험 980개(브라우저 172개 포함).

### `v0.18.0` — 08-25 (마이그레이션 `0040`)

**개체 카탈로그의 카드 하나가 개체 하나가 된다**
([P18](devlog/20260825_P18_object-cards.md) ·
[154](devlog/20260825_154_object-cards.md) · 사용자 지시 2026-08-25).
지금까지 카드는 **후보**였고, `candidate_rows` 가 시야마다 판 하나(합성본이
있으면 합성본)만 훑어서 **합성본에서 안 잡히고 프레임에서만 잡힌 규조각이
카탈로그에 아예 없었다**(106 · 실측 5,123건).

- **모든 판을 훑고 개체별로 한 줄만 낸다.** 묶은 것은 카드 하나, 안 묶은 것은
  판마다 하나 — **기계가 정체를 판정하지 않는다**(P11)
- **아직 아무도 안 본 후보는 카드가 없다.** 개체는 사람이 손대기 전까지 없고,
  **검토 완료가 통과분에 개체를 세운다**(`confirm_kept`). 그래서 겹침으로 접는
  일을 안 해도 된다. 화면이 **"시야 N/M 검토"** 를 적는다 — 조용히 줄면 자료가
  사라진 것으로 읽힌다
- **번호는 앵커에서 파생한다** (`DiatomObject.anchor` · 마이그레이션 `0040`).
  저장하는 것은 **번호가 아니라 재료를 어디서 가져올까**라 `catalog.py` 의
  "저장하지 않는다 — 늘 계산해서 낸다" 가 그대로 선다. **묶어도, ★ 로 대표를
  옮겨도 안 움직인다.** 카드가 내보이는 것은 앵커의 번호 하나지만 **멤버 아무
  번호로나 찾아진다** — 논문에 적어 둔 번호가 나중에도 열려야 한다
- `check_db` — 앵커 검사 둘 (**개체마다 앵커가 있다** · **앵커가 그 개체의
  판정이다**). `/srv` 로 함께 밀린다

- 시험 940 → **954**

### `v0.17.2` — 08-25 (마이그레이션 없음)

**묶음에 지운 판이 섞이는 것을 허용하고, 얼굴만 지킨다**
([151](devlog/20260825_151_removed-member-face.md) · 사용자 방침 2026-08-25).
지우는 문이 둘인데 카탈로그만 거절하고 검토 화면은 통과시키고 있었다 —
**빠뜨린 것이 아니라 결정이었다**(`test_삭제는_안_번진다`). 묶음은 정체에 대한
말이고 오검출 판정은 판마다 다르다는 쪽을 남긴다.

- `link_mains` — **지운 멤버는 개체의 얼굴이 못 된다.** 운영에서 `obj#8094` 가
  그 자리였다: 지운 프레임이 멤버 중 가장 커서 **카탈로그 카드가 그 지운
  마스크로 그려지고 있었다.** 전부 지웠으면 옛 규칙 그대로다
- **대표를 지우면 얼굴을 산 판으로 옮긴다.** 합성본이 있으면 합성본, 없으면
  남은 것 중 가장 크게 보이는 판 — `link_mains` 와 같은 기준이다
- `check_db` — **"묶음에 지운 마스크가 없다" → "묶음의 대표가 지운 판이 아니다"**.
  `/srv` 로 함께 밀린다

- 시험 933 → **940**

### `v0.17.1` — 08-25 (마이그레이션 없음)

**짚어 온 카드를 화면 안으로 옮긴다**
([150](devlog/20260825_150_bring-card-into-view.md)). `v0.17.0` 은 그 카드가
있는 쪽을 여는 것까지였는데, 한 판이 120장이라 **화면 밖이면 사람이 여전히 훑어
찾았다**(사용자 보고).

- 가운데로 스크롤한다. **차례는 안 바꾼다** — 카탈로그는 번호를 따라 읽는 표라
  한 장을 앞으로 빼면 옆 개체와 견주는 일을 못 하게 된다
- 그림이 늦게 뜨면 자리가 밀리므로 다 뜬 뒤에 한 번 더 맞춘다. **사람이
  굴렸으면 그만둔다** — 보고 있던 자리를 빼앗는 것이 못 찾는 것보다 나쁘다
- 카드에 **`눌러서 오신 개체`** 를 적는다. 테두리만으로는 골라 둔 것과 안 갈린다

**`📇 이 개체 카드로` 를 메뉴 맨 아래로 내렸다.** 묶기 바로 아래라 손이 같은
자리로 가서 **검토하다 잘못 눌러 화면이 넘어갔다**(사용자 보고). 이 메뉴에서
`Ctrl+Z` 가 안 듣는 유일한 항목이라, 되돌릴 수 있는 것들 틈에 두지 않는다.
이름에도 적는다 — `(화면을 떠납니다)`.

- 시험 929 → **933**

### `v0.17.0` — 08-25 (마이그레이션 없음)

**종명 칸이 도감을 안다** ([149](devlog/20260825_149_species-atlas-and-back-link.md)).
카탈로그의 종명 칸이 `AtlasEntry` 를 자동완성하고, 적힌 이름이 도감의 자리이면
그 줄에 **`도판`** 이 떠서 오른쪽에 그 쪽을 띄운다(`도감에서 →` 로 큰 화면).

- 새 문 `api/atlas/suggest` — 두 글자부터. **값으로 내는 것은 `binomial` 뿐이다**:
  비어 있다는 것은 도감이 속까지만 내려갔거나(`sp.`·group) 표기를 못 읽는다는
  뜻이라(119), 종명 칸에 주면 갈라 놓은 둘을 화면이 도로 섞는다
- **가두지 않는다** — 도감에 없는 이름도 그대로 적힌다. 단추가 안 뜰 뿐이다
- 도감은 **한 판에 한 번** 묻는다(105). 카드마다 물으면 120번이다

**검토 화면에서 개체 카드로 간다.** 우클릭 메뉴의 `📇 이 개체 카드로` — 118 이
낸 길의 반대 방향이고 열쇠도 같다(`obj`·`img`). 데려다 놓는 것까지가 이
기능이라 파편을 펴 주고, 그 카드가 있는 쪽을 열고, 지운 개체면 「지운 것」 을
연다. 못 찾으면 왜 없는지 적는다(카탈로그는 시야마다 판 하나만 본다 — 106).
읽기 전용에서는 항목을 안 낸다.

- 시험 909 → **929** (3겹 +15 · 브라우저 +5)

### `v0.16.1` — 08-23 (마이그레이션 없음 · 자료만 찬다)

**한국 도감이 도판으로 이어진다** — 색인에 없던 쪽을 재서 표로 두고
(`atlas/korean_pages.toml`) 파서가 채운다
([147](devlog/20260823_147_korean-plate-pages.md)).

```
pdf_plate_page   0/512 → 510/512   (`pl.` 이 없는 #347·#680 은 그대로 비어 있다)
pdf_page       311/512 → 511/512   (#680 은 비운다 — 발췌본이 책 p.369 에서 끝난다)
```

- **원인이 둘이고 서로 별개였다.** ① 색인 머리말이 자리 표기를 넷으로 못박았는데
  (`#`·`pl.`·`책 p.`·`PDF p.`) **도판이 PDF 몇 쪽인지 적는 칸이 없다** — `pl. 57`
  은 책의 도판 번호일 뿐이라 그것으로는 발췌본을 못 연다 ② 481~680 의 200건은
  그 구간만 쪽 이미지로 판독해서 **해설 쪽도 안 따라왔다**
- **색인 원문은 안 고쳤다.** 인용의 근거라 기계가 늘려 쓰면 안 된다 — 잰 것을
  표로 따로 두고 파서가 읽는다(`coredata/mapping.toml` 과 같은 자리)
- **옵셋 99 는 잰 값이다.** 도판 쪽도 책 쪽번호를 함께 먹어서 도판을 지나도
  안 밀린다 — 텍스트 레이어가 있는 113쪽 전부에서 확인했다. **어긋나 보인 둘은
  경계가 아니라 색인의 `책 p.` 오탈자**였다(#238·#424). 표에 적기만 하고
  안 고쳤다
- **셈만 하면 없는 쪽이 생긴다.** 항목 680 은 책 p.370 인데 발췌본이 369 에서
  끝난다 — `AtlasPlacement` 머리말의 "빈 것을 채우지 않는다" 가 그 자리다
- **도판 54개는 읽는 수밖에 없었다**(OCR 이 틀렸던 Schmidt Tafel 114건과 같은
  자리다). 그래서 읽은 값을 그대로 안 앉히고 검산 셋을 세웠다 — 번호가 PDF
  차례대로 오르는가 · 집합이 색인이 인용한 것과 맞는가 · **캡션의 항목 번호가
  색인에서 그 도판을 부르는 항목과 겹치는가**(셋째가 제일 세다). 표를 의심할 때
  `tools/verify_korean_pages.py` 가 원본 PDF 에 대고 다시 잰다
- **템플릿·`data.py` 는 안 건드렸다** — `plate_url` 이 이미 `pdf_plate_page` 를
  보고 있어서 값만 차면 링크가 뜬다(131·141)

시험 **904 → 909**. 뷰어 코드가 안 바뀌어 판을 내는 뜻은 하나다 —
**`atlas/*.json` 이 이미지에 실려 가므로**(`COPY . .` → `/app/atlas/`) 새 이미지가
서야 그 컨테이너의 `ops/import_atlas.py` 가 새 표를 넣는다. **판 먼저, 반입
나중이다.**

08-23 20:00 배포, 20:02 반입(자기 검산 통과 · `check_db` 11번의 "PDF 쪽이 없는
자리" 201건 → 1건). **반입 전에 컨테이너가 어느 JSON 을 보는지 해시로 확인했다**
— 옛 이미지로 돌리면 옛 JSON 을 조용히 넣는다. 그리고 **`#481 Gyrosigma
balticum` 의 도판을 눌러 캡션의 `481` 을 눈으로 봤다**: 자리 수가 맞는 것과
**연 쪽이 맞는 쪽인 것**은 다르다(086).

### `v0.16.0` — 08-23 (마이그레이션 없음)

**도감을 읽을 수 있게 됐다** — 129·131 이 **띄웠고** 이 판이 **읽게** 했다
([141](devlog/20260823_141_atlas-view-controls.md)).

- **도판 두 화면이 조작을 한 조각에서 가져온다**(`_atlasview.html`). 갈라 적으면
  한쪽만 고치는 날이 오고, 그때 **키가 한 화면에서만 듣는다** — 예외도 경고도
  없이 "가끔 안 되는" 것으로 보인다
- `←` `→` 넘긴다 · `g` 격자 · `s` 한 장↔두 쪽 · 휠·`+` `-` `0` 확대 · 끌어서
  이동 · 조작 띠의 `쪽으로 [__]`. **쪽 번호는 `PDF p.N` 이다**(Tafel 은 OCR 값이라
  114건이 틀려 있어 열쇠로 못 쓴다). 없는 쪽이면 격자가 말해 주던 그 갈래로 간다
- **도판이 창 하나에 들어간다.** 예전에는 폭에 맞춰 늘려 놓아 세로로 넘쳤고,
  펼침을 펴 놓고도 한 번에 다 못 봤다. `max-height: 100%` 가 **높이가 `auto` 인
  부모**에 걸려 안 풀리던 자리다 — 745px 창에 2005px 짜리가 서 있었다
- **배율을 %로 말하지 않는다.** 원본 PNG 의 화소 수를 화면이 모른다 — 모르는
  것을 100% 라고 부르면 그 숫자로 판단하게 된다. **곱수(`×2.4`)로 말하고**
  원본을 실제로 물어 왔을 때만 `원본` 을 붙인다. 늘려 보기 시작하면 축소본에서
  원본 PNG 로 갈아 끼운다(`naturalWidth` 로 잰다 — 폭을 코드에 안 박는다)
- **쪽을 넘겨도 배율·자리를 지킨다**(`sessionStorage`, 도감·권·보기마다).
  숨은 상태가 아니다 — 배율 칸에 `×N` 이 뜨고 `[맞춤]` 이 옆에 있다
- **검색 화면의 도감 칩이 속을 떨어뜨리고 있었다.** 속으로 좁혀 놓고 도감을
  바꾸면 조용히 넓어져서, **그 도감에 그 속이 그만큼 있는 줄로 읽힌다.**
  주소를 뷰 한 자리(`_atlas_chips`)로 모았다. **속만 따로 뺄 수 있다** — 예전에는
  `지운다` 뿐이라 검색어까지 같이 날아갔다
- **`해설 p.`·`도판 p.` 칩에 미리보기**. 예순 건 중 어느 쪽인지 고르려고 눌러
  들어갔다 나오기를 되풀이하던 자리다. **디스크를 안 짚는다** — 경로만 내려보내고
  띄울 때 주소를 만든다(링크마다 `stat` 이면 한 판에 수백 번이다). 안 구운 쪽이면
  빈 칸을 남기지 않고 말한다
- 결과 위에도 앞뒤를 뒀다(한 판이 50줄이다)

시험 **891 → 904**. 되살려 보고 둘을 고쳤다 — 새 시험 하나가 **픽스처 도판이
창보다 작아** 높이 제약을 한 번도 안 지나고 있었고, 131 의 펼침 시험은 재던
요소가 캔버스를 겸하게 되어 **실패할 수 없게** 됐길래 낱장으로 되돌렸다.

08-23 19:33 배포. **smoke 가 안 여는 화면을 손으로 열었고**, 도감 넷은
브라우저로 눌러 확인했다(키 넘기기·휠 확대·미리보기가 운영에서 돌고 JS 오류
없음) — 200 은 "떴다" 일 뿐이라 그것만으로는 안 된다(086).

### `v0.15.0` — 08-19 (마이그레이션 `0039`)

**코어 자료가 깊이 축 옆에 선다** — 시추코어의 물성·지화학을 지점 화면에
꺾은선으로 그리고, 사람이 넣고, 관찰에서 세어 온다
([133](devlog/20260819_133_core-profile-import.md)·[135](devlog/20260819_135_core-profile-chart.md)·[136](devlog/20260819_136_profile-into-corelog.md)·[137](devlog/20260819_137_core-manual-entry.md)·[138](devlog/20260819_138_core-derived-series.md) · [P17](devlog/20260819_P17_core-profiles.md)).

- **표 둘** — `CoreSeries`(측정 항목) + `CorePoint`(점). 칼럼을 고정한 표로 두지
  않았다: **코어마다 있는 항목이 다르다**(RS14 만 XRF·Opal, RS19 만 Spectro).
  깊이는 **mm 정수**다 — `(항목, 깊이)` 가 유일 제약의 열쇠라 부동소수면 안 된다
- **세 갈래가 한 목록에 선다** — `import`(xlsx) · `manual`(사람) · `derived`(관찰
  에서 셈, **저장 안 됨**). 재반입은 `source='import'` 만 갈아치우므로 사람이
  넣은 값이 안 지워진다
- **반입은 두 단이다** — 호스트에서 xlsx → 중간 CSV(`tools/coredata_extract.py`),
  컨테이너에서 CSV → DB(`ops/import_coredata.py`). 웹 이미지에 `openpyxl` 이 안
  들어간다. **머리글의 단위를 안 믿는다** — `coredata/mapping.toml` 이 근거이고,
  `RS14-GC04` 는 `MS`·`Opal`·`Size` 셋이 머리글과 실제가 달랐다
- **축의 근거가 넓어졌다** — `max(시료 깊이, 항목의 최대 깊이)`. **관찰이 없는
  코어에도 축이 선다**(새 코어 둘이 그 상태로 들어온다)
- **점이 800개를 넘으면 줄이되 값을 지어내지 않는다** — 구간마다 실제로 잰 점의
  최소·최대를 남기고(평균을 내지 않는다) **줄였다고 화면이 적는다**.
  빈 구간은 안 잇는다(중앙 간격의 4배부터 끊는다)
- **넣는 문이 둘이다**(116) — 항목의 속성과 점 목록을 가른다. 붙여넣기는 미리
  보기를 거치고 **같은 파서를 지난다**. 반입 항목에는 못 넣는다
- 화면: `?series=` 로 켠 항목이 주소에 남는다(**빈 값은 "다 끄기"**). XRF 24개
  원소는 접어 뒀다
- **지점을 지울 때 코어 자료를 센다**([139](devlog/20260819_139_delete-threshold-core-data.md)).
  `CASCADE` 라 예외도 경고도 없이 함께 지워지는데 **새 코어는 시료가 0개라**
  문턱이 그냥 열렸다 — 예행에서 27개 항목 10,795점이 조용히 날아갔다.
  대신 **반입 항목은 지울 수 있게** 했다(고치는 것과 다르다 — 다시 반입하면
  돌아온다). 안 그러면 반입 자료가 붙은 지점을 영영 못 지운다

08-19 18:37 배포. 지점 `RS14-GC04`·`RS19-GC17` 을 세우고 **99,453점을 반입했다**
([140](devlog/20260819_140_v0150-release.md)).

### `v0.14.0` — 08-18 (마이그레이션 `0038`)

**도감이 들어왔다** — 색인 셋의 표제어 **2,059**를 DB 에 넣고, 도판 **1,336쪽**을
화면에서 넘겨 본다 ([128](devlog/20260818_128_atlas-parsers.md)·[129](devlog/20260818_129_atlas-plate-viewer.md)·[130](devlog/20260818_130_atlas-schema-and-import.md)·[131](devlog/20260818_131_atlas-search.md) · [P15](devlog/20260813_P15_atlas-db.md)).

- **표 셋** — `Atlas`·`AtlasEntry`·`AtlasPlacement`. 자리를 항목의 칸으로 두려던
  계획을 고쳤다: **Schmidt 254건이 자리를 여럿 갖는다**(최다 11)
- **반입은 두 단계다** — 호스트에서 색인 md → `atlas/*.json`(`tools/parse_atlas.py`),
  컨테이너에서 JSON → DB(`ops/import_atlas.py`). 뷰어 컨테이너가 NAS 를 못 봐서
  가른 것이고, JSON 이 저장소에 있으니 **색인이 바뀐 것이 diff 로 보인다**
- **원본은 md 이고 DB 는 사본이다.** 반입은 멱등이고 도감마다 통째로 갈아치운다 —
  **`AtlasEntry` 에 FK 를 매달면 안 된다**. 사람이 만든 학명 유효성 판정은 이 표에
  안 담는다(뒤에 `TaxonName` 으로 온다)
- **검산이 두 겹이다** — 색인↔JSON, JSON↔DB. 표제어는 한 글자까지 본다.
  반입이 원문 오식(`Triceratium venustun`)을 조용히 고치면 도감을 인용할 수 없다
- **화면** — `/atlas/` 목록·검색(표제어·이명법·속), 쪽 격자, 쪽 한 장,
  **두 쪽 펼침**(홀짝이 도감마다 달라 자료로 갈랐다). 도판 주소를 만드는 자리는
  `viewer/atlas.py` 하나다
- **`(속명 추정)` 을 "확정" 이라 말하지 않는다** — 표시가 없는데 잘못 펴진 것이
  있다(119). `sp.`·`group`(속까지 동정)과 **이름이 상해 못 읽는 것**도 갈라 말한다
- `ops/check_db.py` **11번** — `ClassDef`(is_taxon)의 속과 개체 종명이 도감에 있는가
- 시험 644(브라우저 뺀 것) · 797(전부). 도감만 40개다

**반입은 배포 뒤에 따로 돈다** — 마이그레이션은 표를 세울 뿐 비어 있다.
08-18 18:00 배포 · 17:57 반입 완료(HANDOFF 3.8). **그 배포가 읽기 전용 DB 로
한 번 엎어져 운영이 3분 내려갔다** ([132](devlog/20260818_132_readonly-wal-outage.md)).

### `v0.13.1` — 08-14 (마이그레이션 `0037`)

분류에 **Rhizosolenia** 를 더하고, 검토 화면에서 **깊이 맵을 뺀다**
([122](devlog/20260814_122_rhizosolenia.md)·[123](devlog/20260814_123_hide-depth-map.md)).

- **`Rhizosolenia`** — `Eucampia`·`Chaetoceros` 와 같은 층(속)이다. 지금까지
  **봉상으로만 갈라 온 것**을 속으로 빼냈다. 약칭 `RHZ` · 단축키 **`t`** ·
  색은 연보라(`185,130,255`). **이미 붙은 교정은 안 건드린다** — 옮기는 것은
  검토 화면에서 사람이 한다
- **검토 화면 캐러셀에서 깊이 맵을 안 낸다.** Z 좌표가 없어 상대값이고 검출이
  붙지 않는 판이라 골라도 볼 것이 없다 — 116 에서 저장이 대표 이미지로 가던
  그 판이다. **자료는 그대로 둔다**(`Stack.depth_path`·`Image`·`depth_rel`)
- 고침: **개수 줄에서 `Chaetoceros` 가 빠져 있던 것.** 그 줄이 분류 다섯을 손으로
  박아 두어 `v0.10.x` 이후 내내 안 세어졌다. 이제 표의 차례를 따르므로 **다음
  분류는 코드를 안 고쳐도 낀다**

**마이그레이션 `0037` 은 행 하나를 더하는 것이라 파이프라인과 함께 올릴 필요가
없다** — 스키마를 조이지 않고, 파이프라인은 `ClassDef` 에 쓰지 않는다.

시험 13개를 더했다 — 전체 757개.

### `v0.13.0` — 08-14 (마이그레이션 없음)

개체 카탈로그에서 **지우고, 골라서 한 번에 고치고, 묶음을 푼다**
([P16](devlog/20260814_P16_catalog-edit.md)·[120](devlog/20260814_120_catalog-edit.md)).

- **카드마다 `지운다`.** 지운 카드는 **그 자리에서 잠기고 `되살린다` 로 바뀐다** —
  거르개까지 가지 않아도 되돌린다. 잘못 누를 수 있는 단추라 되돌릴 자리를 같은
  카드에 두었다
- **`지운 것` 칩**(`?gone=1`)으로 지운 개체만 따로 본다. **거기서는 파편을 안
  감춘다** — 살아 있는 교정의 65%가 파편이라, 감추면 지운 것의 절반이 되살릴
  자리를 잃는다. 진행도 막대는 **언제나 통과분 기준**이라 분모가 안 흔들린다
- **일괄 편집** — 카드를 골라 종명·유형·등급·자세를 한 번에. 여기만 `적용` 을
  눌러야 가고(카드 칸들은 누르는 즉시 간다), **한 트랜잭션으로 안 묶어** 하나가
  걸려도 나머지가 저장된다. 실패한 카드에만 이유를 적는다.
  **지우기와 코멘트는 일괄에 없다**
- **묶음 풀기** — 카드의 `묶음 N 풀기`. **묶는 것은 없다**: 묶음 멤버는 서로 다른
  판이어야 하는데 카탈로그는 시야마다 한 판만 카드로 내 **묶을 상대가 화면에 없다**
- 등급·자세가 붙은 개체를 지울 때 **무엇이 남는지 묻는다** — 삭제는 되돌릴 수
  있고 등급·자세는 재생성 불가라, 되돌릴 수 있는 일에 되돌릴 수 없는 일을 안 얹는다

저장은 `POST /d/<슬러그>/catalog/save` 하나에 `act` 로 갈린다
(`save`·`remove`·`restore`·`bulk`). **`/review` 에 안 싣는다** — 그 길은 범위를
갈아치우는 전제 위에 있고 두 번 사고 낸 자리다.

시험 30개를 더했다(브라우저 7개 포함) — 전체 744개.

### `v0.12.3` — 08-13 (마이그레이션 없음)

링크가 짚어 온 개체를 검토 화면이 표시한다
([118](devlog/20260813_118_highlight-linked-object.md)).

- **카탈로그·크롭 화면·계측 표에서 사진을 누르면 그 개체를 표시한다.** 주소가
  개체를 함께 나른다(`?obj=<개체 키>&img=<판>`) — 시야 하나에 개체가 수십 개라
  **어느 것을 보고 눌렀는지 다시 찾아야 했다**
- 개체가 다른 판(프레임)에 있으면 **캐러셀이 그 판으로 옮겨 간다**
- 알림 줄의 `이 개체로 ⤢` 로 센터+확대한다 (원본 화소는 안 넘긴다)
- **못 찾으면 그렇게 적는다** — 낡은 링크로 404 를 내지 않는다

**`smoke.sh` 가 이것을 못 본다** — 링크를 눌러 보지 않아 기능이 죽어 있어도
일곱 화면은 200 이다(086 과 같은 구멍). 배포 뒤 손으로 볼 것은 절차 8번에 적었다.

같은 판에 **도감 쪽 작업**이 함께 들어갔지만 **뷰어 이미지에는 안 실린다**
(`devlog/`·`tools/` 와 저장소 밖 NAS 문서다) — 동남극 도판집 학명 색인
([117](devlog/20260813_117_east-antarctic-plate-index.md)) · 도감을 DB 로 넣는 계획
([P15](devlog/20260813_P15_atlas-db.md)) · Schmidt 색인이 속명을 잘못 편 자리
([119](devlog/20260813_119_schmidt-genus-restoration-bug.md)) ·
`tools/harvest_worms.py`.

### `v0.12.2` — 08-13 (마이그레이션 없음)

검토 완료가 서버에 안 가는 갈래와, 번지기가 옮겨 놓은 개체의 대표
([116](devlog/20260813_116_done-lost-and-drawn-rep.md)).

- **검토 완료를 판 payload 에서 뗐다.** 완료는 `(시야, 묶음)` 한 줄인데 판 단위
  교정과 한 요청으로 가고 있었다 — 표시 하나를 켜는 일이 그 판의 교정을
  갈아치우는 일이었다. 커서가 캐러셀의 판 위에 남은 채 누르면 **화면만 켜지고
  서버는 못 받았고**, 검출이 없는 판(깊이 맵)을 고른 채 누르면 **대표 이미지의
  교정이 지워졌다**. 이제 `{"only": "done"}` 으로 한 줄만 쓴다
- **떠나기 전 저장을 기다린다.** 실패하면 안 떠난다 — 떠나면 실패 띠를 볼
  사람이 없다. 엔진 라디오도 같은 문으로 보낸다
- **번지기의 대표는 합성본이다.** `src`(저장할 때 열려 있던 판)를 대표로 삼아
  개체 아홉이 흐린 단일 프레임을 얼굴로 갖게 됐다 — 카탈로그 크롭과 학습
  자료로 뽑히는 판이다
- 배포 뒤 **`migrate/refix_drawn_reps.py`** 로 이미 옮겨간 대표를 되돌린다
  (순서: 사본 → 배포 → 스크립트)

### `v0.12.1` — 08-13

**여기가 운영이 아니라는 것을 화면이 말한다** (`.envwm` ·
[115](devlog/20260813_115_env-watermark.md)). `DIARUGA_ENV_LABEL` 이 있으면 그
글자가 화면 전체에 대각선으로 얹힌다 — 테스트를 운영으로 알고 한 검토는 아무
데도 안 남는다. `DEBUG` 가 켜져 있으면 이름을 안 적어도 뜬다.

### `v0.12.0` — 08-12 (마이그레이션 `0035`·`0036`)

**`v0.11.2` 를 먼저 내보낸 뒤다** — 그 판은 등급이 *판정에* 있는 판이고 `0035` 가
그것을 개체로 옮긴다. 순서가 바뀌면 안 된다.

**뷰어만 올린다.** 두 마이그레이션이 걷는 칼럼(`ObjectReview.grade`·`note`)을
파이프라인이 안 쓴다 — 그래도 조인 사본에 파이프라인 컨테이너를 붙여 한 번 돌려
본다(055 가 그렇게 잡았다).

- **그린 마스크가 같은 시야의 모든 판에 번진다** (106 2·3단계 · 111).
  YOLO 로 갈아탄 뒤 판마다 같은 마스크를 다시 그리던 일이 한 번으로 준다.
  되는 근거는 **촬영 방식**이다 — 스테이지가 안 움직여 좌표가 그대로 맞는다.
  **서버 + 화면을 한 커밋으로** 넣었다(반쪽이면 복제가 다른 판의 저장에서 사라진다)
- **묶인 그 순간에 사슬이 뜬다** — `links` 는 페이지가 열릴 때 한 번 받는 값이라
  새로고침 전에는 배지가 안 떴다. 방금 그린 것은 **잠깐 밝힌다**(`linkflash`)
- **등급을 판정에서 개체로 옮긴다** (`0035` · 111). 하루 만에 뒤집었다 —
  *"어느 판이 잘 보이는가" 는 `is_rep`(대표)이 이미 하고 있었다.*
  운영에 0건이라 지금이 가장 쌌다
- **코멘트도 개체로 옮기고, 적는 자리를 개체 카탈로그 하나로 모은다**
  (`0036` · 112). 사람이 적은 것은 판이 아니라 **그 규조각에 대한 말**이었고,
  번지기 뒤로는 같은 말을 판 넷에 적게 된다. 검토 화면에서 목록·팝업·우클릭
  항목을 **전부 걷었다**(저장만 잠그는 것으로는 안 된다 · 051) — **읽는 것은
  그대로다**. 시야 코멘트는 다른 칸이라 남는다
- **묶을 때 코멘트는 거절하지 않고 잇는다** — 분류·종명은 값을 하나 골라야 뜻이
  서지만 글은 이어도 글이고, 사람이 쓴 글은 재생성 불가다
- 고친 것 — **묶음을 풀 때 등급·자세가 사라지던 것**(`_split_off` 가 0034·0035 를
  따라가지 않았다) · 목록의 "코멘트 N" 이 판을 세던 것(개체를 센다)
- 시험 662개(브라우저 포함). 브라우저 시험이 걷다 만 `closeNoteEdit` 호출을 잡았다

### `v0.11.2` — 08-12 (마이그레이션 `0034`)

**뷰어만 올린다** — 두 칸이 `db_default` 라 판이 다른 옛 파이프라인 이미지의
INSERT 가 안 멈춘다. 올린 뒤 `dbsync.sh check_db.py` 로 10번 검사를 옮긴다
(**순서가 있다** — 먼저 옮기면 옛 `/app` 코드가 모르는 칸을 물어 죽는다).

- **등급·자세** (110) — 우수한 개체를 골라 먼저 학습시키기 위한 두 칸.
  **등급(`A`·`B`·`C`)은 판정(`ObjectReview`), 자세(`valve`·`girdle`·`other`)는
  개체(`DiatomObject`)** — 초점을 옮기면 달라지는가로 갈랐다
- 파편에는 두 칸을 안 준다 — 화면에서 감추고 **서버가 다시 검사한다**.
  유형을 파편으로 바꾸면 조용히 지우지 않고 **묻는다**
- 카탈로그 카드에 두 칸 · **파편 기본 감춤 + 체크박스** ·
  진행도의 "동정" 이 **종명·등급·자세 셋**을 요구한다(배포 직후 0% 로 보인다)
- `/review` 청소가 두 칸만 있는 행을 안 지운다
- **`ops/export_review.py` 가 두 칸을 낸다** — 사람이 매긴 것이라 재생성 불가다.
  **매긴 것에만 실어 형식 번호는 그대로 `4`** (`species` 와 같은 규칙)
- **`ops/check_db.py` 10번** — 파편·지운 판에 두 칸이 붙어 있지 않은가.
  화면·서버 다음의 **세 번째 자리**다 (옆문은 예외를 안 낸다)
- `0033` 과 번호가 부딪혀 **`0034` 를 뒤로 줄 세웠다**(둘 다 칼럼 추가라 순서가
  뜻을 안 바꾼다)
- 문서 — README 를 소개·구성만 남기고 셋을 `docs/` 로 · "밟기 쉬운 곳" →
  **"자주 빠지는 함정"** 이고 목록을 `CLAUDE.md` 한 곳으로 · HANDOFF 에서 연대기와
  중복 규약을 걷는다(이 파일이 그 연대기의 새 집이다) · 진척 보고서 11차 ·
  하루치 브랜치 규약 · 새 작업자 붙이기

### `v0.11.1` — 08-11 (마이그레이션 `0033`)

- **검토 완료가 남는 개체에 확인 표시를 단다** (`auto_confirmed` · 109).
  그때까지 *"이건 규조각이 맞다"* 는 판단이 **행의 부재로만** 기록됐다
- **`accepted`(되살림)와 축이 달라 칸을 따로 뒀다** — 뭉치면 "엔진이 놓친 것
  몇 건" 을 다시 못 센다. 화면은 이 칸을 모르고 **서버가 청소에서 스스로 뺀다**
- 대상을 좁은 규칙에서 **남는 개체 전부**(통과분 + 되살린 탈락분)로 넓혔다 (109 덧)
- 캐러셀을 스칠 때 그 판의 사슬을 그린다 (108 덧 6)

### `v0.11.0` — 08-11 (마이그레이션 `0032` · **파이프라인 `v0.5.2` 와 함께**)

- **개체와 판정을 가른다** — `ObjectReview` + `ObjectLink` + `ObjectLinkMember` 가
  **`DiatomObject` + `ObjectReview`** 둘로 모였다. **판정 행이 곧 멤버**이고
  분류·종명은 개체에 산다 (P12 · 108)
- 104 의 **전파 코드가 사라졌다** — 진실이 하나면 번지게 할 것이 없다
- 읽는 자리 79곳은 **읽기 전용 통로**로 그대로 뒀다(쓰면 `AttributeError`)
- 묶기가 **엇갈리는 값을 안 고르면 409 로 거절한다** — 개체가 값을 하나만 든다
- 고친 것 여섯 (108 덧 1~5): **`/review` 청소가 묶음 멤버를 지우던 것** ·
  **묶기가 판정을 거짓 `orphan` 으로 세우던 것** · 있는 행의 바인딩 다시 맺기 ·
  묶임 표시를 뚜렷하게(26px)
- **칼럼을 걷는 마이그레이션이라 파이프라인을 함께 올렸다** — 옛 판은 교정 있는
  시야를 지울 때 `FOREIGN KEY constraint failed` 로 죽는다(조인 사본에서 실측)
- 교정 감사 기록(`review/`)을 이전기 뒤로 다시 맞췄다

### `v0.10.2` — 08-10

- **묶을 때 분류·종명이 엇갈리면 알리고 고르게 한다** (107). 묶기 *전에* 판마다
  적어 둔 것은 104 가 못 건드린다 — 운영의 묶음 셋 중 둘이 어긋나 있었다.
  **기본은 `그대로`**(P12 뒤로는 성립하지 않아 409 가 됐다)
- DB 명세·ERD 를 그날 모습으로 다시 구웠다

### `v0.10.1` — 08-10

- **카탈로그에서 유형을 바꿔도 묶음에 번진다** (105 덧) — 번지게 하는 코드가
  `save_review` 안에만 있었다
- `geom` 스냅샷의 키가 두 벌이던 것(`bbox` / `bbox_xywh`)을 `geom_box()` 로 모았다
- **YOLO 에서 카탈로그의 전제가 깨지는 것**을 확인해 적어 뒀다 (106) — SAM 은
  시야마다 판이 하나지만 YOLO 는 합성본 1장 + 프레임 2~10장이다

### `v0.10.0` — 08-10 (마이그레이션 `0031`)

- **개체 카탈로그** (`/d/<슬러그>/catalog/` · 105) — 카드 하나가 개체 하나
- **카탈로그 번호는 저장하지 않고 파생한다**(층·시야·bbox·묶음 코드).
  칼럼에 넣으면 층을 고치거나 재검출할 때 **예외 없이 그냥 틀린다**
- **종명(`species`) 칸이 처음 생겼다** — 자유 입력 + 자동완성
- `RunBatch.code`(`S1`·`Y3`)를 사람이 정하게 했다 — 번호 꼬리가 된다
- **검토 저장이 동정을 지우던 갈래**를 막았다 — `/review` 가 모르는 값은 청소에서 뺀다

### `v0.9.4` — 08-10

- **묶인 개체는 분류를 함께 받는다** (104). 묶음은 "이 판들의 이것이 같은 개체다"
  이므로 유형은 개체의 성질이다 — **삭제·되살림·코멘트·기하는 안 번진다**

### `v0.9.3` — 08-10

- **프레임 판으로 열린 시야 11개에서 교정이 통째로 거절되던 것**을 고친다 (103).
  덧문이 "시야의 현재 검출" 중 아무거나 하나를 집었는데 **시야 하나에 현재 검출이
  여럿이다.** 그 열한 화면에서는 **어떤 교정도 저장된 적이 없었다**
- 저장 실패를 **안 사라지는 띠**로, 서버의 말 그대로 적는다
- 되살린 탈락분을 화면 상태에도 옮긴다 (102 덧 2)

### `v0.9.2` — 08-10 · **git 태그가 없다**

- **묶기 팝업이 탈락 후보까지 낸다 — 고르면 되살아난다** (102). 초점이 흐린 판은
  텍스처가 모자라 탈락하는데 **그 프레임에서 가장 좋은 마스크가 그것일 때가 있다**
- 되살리기는 **서버가 한 행만 좁게** 한다(화면에서 하면 `/review` 가 그 이미지의
  교정 전체를 갈아치운다)
- 탈락 후보를 고르면 같은 것이 하나 더 나타나던 것을 고친다 (102 덧)

### `v0.9.1` — 08-10

- **저장소를 넷으로 가르고 운영을 `/srv` 하나로 세운다** (100 · 101) —
  `pipeline/`·`ops/` 는 `/srv` 로 가고 `migrate/`·`tools/` 는 안 간다.
  **운영 서버에 저장소가 없어도 돈다**
- 이미지에서 `/srv` 스크립트를 뽑는 갈래 (`sync_to_srv --from-image` · 100 덧)
- **`APP` 은 `sys.path` 뒤에 붙인다** — 앞이면 이미지 안의 옛 `judge.py` 가
  `/srv` 의 새것을 가린다 (100 덧 2)

### `v0.9.0` — 08-10 (마이그레이션 `0030`)

- **같은 개체 묶기** (P11 · 090·091·099) — 우클릭 → 판마다 근처 후보를 하나씩
  고르고 별로 대표를 세운다. **사람이 고르는 순간 재생성 불가 자료**가 되어
  교정과 같은 규약으로 갔다(원안인 자동 계산 층은 뒤집혔다)
- 문턱을 **이웃 이미지 쌍 13,523곳에서 실측**하고 스키마를 정했다 (090)
- **파이프라인 상태 화면** — 시스템 설정 · 파이프라인 탭 (098)
- **폴러 3단계가 heredoc 굶김으로 사흘째 죽어 있던 것**을 고친다 (097).
  "조용히 성공한 빈 결과" 와 "실패" 를 한 갈래로 접지 않는다
- 저장 실패가 조용히 눌러앉아 그린 마스크가 묶이지 않던 것 (096)
- 감사 기록 **FORMAT 4** (묶음을 싣는다) · 진척표에 고친 마스크 수(`geom_edited`)
- 남극 **2차 검토를 시작**하며 완료 표시를 걷었다 (092) · `Chaetoceros` 첫 대거
  동정 (093)

### `v0.8.3` — 08-09

- **검토 중인 묶음을 화면이 말한다** (088) — 목록·시야 목록·크롭·계측 표까지
- **시스템 설정을 최상위 메뉴로** 올리고 이름을 `system-settings` 로 끝까지
  맞췄다 (089 · 옛 `/manage/*` 는 301)
- 테스트 배포를 운영과 같은 갈래로 관리한다 (087)
- 시야 목록의 링크 셋을 제목 줄로 모으고 문구를 분명히 했다

### `v0.8.2` — 08-07

- **크롭 갤러리와 계측 표가 죽어 있던 것을 고친다** (086) — **v0.8.0 이후 내내
  500 이었다.** URL 을 덮는 시험은 셋이나 있었지만 자료가 전부 합성본이라
  **프레임 갈래를 한 번도 안 지났다**
- **테스트 인스턴스** `/DiaRUGATest/` (085) — 사본 DB 로 돈다
- **묶음을 화면에서 만든다** (084) · 관리 화면을 셋으로 가른다 (083)
- 마스크를 그릴 때 마지막 점에서 커서까지 점선 (082)
- Docker 저장소를 `/data3` 로 옮긴다 (081) · smoke 가 `/srv` 스크립트 표류를
  알린다 (080)

### `v0.8.1` — 08-07

- **묶음마다 조리법으로 새 자료를 채운다** (`RunBatch.recipe` · 079)
- **묶음을 골라 재판정한다** (`refilter.py --batch` · 078)
- 판정 캐시가 어긋나던 자리 — **판정 앞에서 접는다** (077)
- 감사 기록 **FORMAT 3** · 가중치도 NAS 로 보낸다

### `v0.8.0` — 08-07

- **마스크를 그리고 고친다** (P09) — 사람이 직접 그린 개체가 자료가 된다
- **검토할 묶음을 화면에서 고른다** (P10 · `RunBatch.for_review`) —
  뷰어가 볼 것을 정하는 것이 `is_current` 에서 이쪽으로 옮겨졌다
- **검토 완료를 묶음마다 남긴다** (073) — 회차가 다르면 다시 봐야 한다
- 파이프라인을 묶음의 뜻에 맞춘다 (076) · **`/engine/` 화면을 지운다** (075 —
  라디오가 같은 일을 자리를 안 잃고 한다)
- 커서와 클릭이 같은 개체를 가리키게 (072) · 빈 화면이 왜 비었는지 말하게

### 그 앞 — 태그가 없다

- **`v0.7.0`** (08-06) — **자료의 층을 말과 맞췄다.** `Core` → `Locality`(지점),
  그 아래 `Sample`(시료)을 새로 뽑았다. **지금 구조의 뿌리다** (063)
- 그보다 앞은 `devlog/` 001~062 와 진척 보고서 1~7차에 있다 — DB 이전(P02) ·
  컨테이너와 NAS 자동 수집(P03) · YOLO 학습(P04) · `Image` 정규화(P06) ·
  분류 테이블(038~040) · 뷰어 이름을 `DiaRUGA` 로(041·048).

---

## 파이프라인

**git 태그가 없다** — 이미지 태그(`PIPELINE_TAG`)로만 있다. 뷰어와 굽는 주기가
다르고, **함께 올려야 하는 때만** 판이 움직인다.

### `v0.5.2` — 08-11

- P12 가 칼럼을 걷어 **뷰어 `v0.11.0` 과 한 창에서 올렸다**. 옛 판(`v0.5.1`)은
  교정이 있는 시야를 지울 때 `FOREIGN KEY constraint failed` 로 죽는다 —
  **조인 사본에 옛 컨테이너를 붙여 실측했다**(교정이 없는 시야로는 그냥 통과한다)
- 올릴 때 폴러를 `flock /tmp/DiaRUGA-poll.lock` 으로 잡았다 — **crontab 은 안 고친다**

### `v0.5.1` — ~08-10

- 뷰어 `v0.9.0`~`v0.10.2` 는 **칼럼을 걷지 않아** 이 판 그대로 갔다
  (마이그레이션 `0030`·`0031` 은 더하기만 한다)

### `v0.4.0` — 08-06

- 층 재편(`Core` → `Locality`) 때 뷰어와 **함께** 올렸다.
  **슬러그·경로 같은 "값의 모양" 규칙은 파이프라인이 만들고 뷰어가 쓴다**(057) —
  코드만 고치고 이미지를 안 올리면 옛 규칙이 계속 값을 만들고, 규칙을 어긴 값이
  DB 에 앉으면 **그 값을 읽는 화면이 전부 죽는다**

---

**앞으로 내는 판은 낼 때 여기 한 항목 더한다.** 여기 없는 옛 판은 "안 나간 것" 이
아니라 **이 표를 08-11 에 세웠을 때 태그로 남아 있던 것부터 적었다**는 뜻이다.
